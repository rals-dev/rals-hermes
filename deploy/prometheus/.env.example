# Address Prometheus is published on. The LAN IP lets Grafana on svrdocker
# add http://<lan-ip>:9090 as a data source. 127.0.0.1 keeps it local only.
PROM_BIND=<lan-ip>
PROM_RETENTION_TIME=30d
PROM_RETENTION_SIZE=2GB
