# Traefik host rule, same pattern as the dashboard.
GRAFANA_HOST=grafana.srv01-rals.<tailnet>.ts.net

GRAFANA_ADMIN_USER=admin
# openssl rand -base64 24
GRAFANA_ADMIN_PASSWORD=

# Loki on svrdocker (only if Alloy ships logs there); empty = no data source.
LOKI_URL=http://<svrdocker-lan-ip>:3100
